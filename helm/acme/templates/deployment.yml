apiVersion: apps/v1
kind: Deployment
metadata:
  name: "acme-app"
spec:
  selector:
    matchLabels:
      app: "acme-app"
  replicas: {{ .Values.replicaCount }}
  template: 
    metadata:
      labels:
        app: "acme-app"
    spec:
      containers:
      # - image: techtestapp:latest
      - image: {{ .Values.image }}
        name: "acme-app"
        env: 
          - name: VTT_DBHOST
            value: {{ .Values.dbhost }}
          - name: VTT_DBUSER
            value: {{ .Values.dbuser }}
          - name: VTT_DBNAME
            value: {{ .Values.dbname }}
          - name: VTT_DBPASSWORD
            value: {{ .Values.dbpassword }}
        ports:
        - containerPort: 80